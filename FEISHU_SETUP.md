# 飞书自建应用发布说明

本文档用于把“学生时间规划表图片生成器”发布成飞书企业自建应用，供同事在飞书工作台中使用。

## 一、当前工具形态

当前网页是纯静态工具：

- 上传 Excel 后在浏览器本地解析
- 在页面中编辑、拖动、拉伸、删除或新增色块
- 在浏览器本地导出 PNG 或 PDF
- 不上传 Excel 到服务器
- 不调用飞书 API
- 不需要配置 App Secret、回调地址或服务端接口

因此飞书侧只需要配置“网页应用”入口。

## 二、发布前准备

### 1. 准备 HTTPS 访问地址

把整个 `timeline_web/` 目录部署到一个 HTTPS 静态网站地址。

可选方式：

- 公司内部静态网站服务
- 阿里云 OSS / 腾讯云 COS / 七牛云静态网站
- Vercel / Netlify / Cloudflare Pages
- 其他可被同事电脑和手机访问的 HTTPS 地址

最终需要得到类似下面的地址：

```text
https://your-domain.example.com/timeline/
```

注意：

- 不建议使用 `file://` 本地文件地址发布给同事。
- 飞书工作台中的网页应用需要同事客户端能访问该地址。
- 如果公司网络有限制，需要确认飞书 PC 端和移动端都能打开该 HTTPS 地址。

### 2. 确认依赖加载

当前页面从 CDN 加载两个浏览器库：

- SheetJS：读取 Excel
- jsPDF：导出 PDF

如果同事网络可能无法访问 CDN，建议正式发布前把这两个文件下载到 `vendor/` 目录，并把 `index.html` 中的引用改成本地路径。

推荐目标结构：

```text
timeline_web/
  index.html
  styles.css
  app.js
  vendor/
    xlsx.full.min.js
    jspdf.umd.min.js
```

对应把 `index.html` 中的引用改为：

```html
<script src="./vendor/xlsx.full.min.js"></script>
<script src="./vendor/jspdf.umd.min.js"></script>
```

## 三、创建飞书企业自建应用

### 1. 进入飞书开放平台

打开飞书开放平台开发者后台：

```text
https://open.feishu.cn/app
```

使用企业账号登录。

### 2. 创建企业自建应用

在开发者后台首页点击：

```text
创建企业自建应用
```

建议填写：

```text
应用名称：学生时间规划表生成器
应用描述：上传学生时间规划表 Excel，在线编辑计划色块，并导出 PNG/PDF。
应用图标：使用星屿品牌图标或一个简洁表格/日历图标
```

创建完成后进入应用详情页。

## 四、添加网页应用能力

在应用详情页左侧进入：

```text
应用能力 -> 添加应用能力
```

选择并添加：

```text
网页应用
```

进入网页应用配置页后，填写主页地址。

### 桌面端主页

填写你部署好的 HTTPS 地址，例如：

```text
https://your-domain.example.com/timeline/
```

推荐默认让它在飞书客户端内打开。如果导出下载在个别电脑上受限，可改成让主页跳转浏览器打开，在 URL 后拼接：

```text
?lk_jump_to_browser=true
```

示例：

```text
https://your-domain.example.com/timeline/?lk_jump_to_browser=true
```

### 移动端主页

如果移动端也要使用，填写同一个 HTTPS 地址。

说明：

- 这个工具主要用于上传 Excel 和下载图片，PC 端体验最好。
- 移动端可以预览，但编辑大型横表不如电脑方便。

## 五、安全设置

当前工具不调用飞书 JSAPI，所以通常不需要申请权限。

如果未来要接入以下能力，就需要继续配置飞书权限和服务端：

- 获取当前飞书用户身份
- 把导出的图片自动上传到飞书云盘
- 自动发送图片到飞书群
- 保存用户历史项目

如果未来需要使用飞书 JSAPI，需要在：

```text
安全设置 -> H5 可信域名
```

添加网页所在域名，例如：

```text
your-domain.example.com
```

## 六、发布应用

进入应用详情页左侧：

```text
应用发布 -> 版本管理与发布
```

点击：

```text
创建版本
```

填写版本说明，例如：

```text
首次发布：支持上传 Excel、编辑计划色块、拖拽调整、导出 PNG/PDF。
```

提交发布。

企业自建应用一般需要企业管理员审核。审核通过后，同事就可以在飞书工作台中使用。

## 七、管理员配置

如果同事在工作台看不到应用，请让企业管理员检查：

- 应用是否已发布上线
- 应用是否审核通过
- 应用可用范围是否包含目标同事或部门
- 工作台是否已展示该应用

## 八、上线验收清单

发布后用一份真实 Excel 做验收：

- 能在飞书工作台打开页面
- 能上传 `.xlsx`
- 标题、阶段、年级、月份显示正常
- 原有色块显示正常
- 能新增色块
- 能删除选中色块
- 能拖动色块到其他月份或栏目
- 能通过四边手柄拉伸色块
- 能编辑色块文字
- 能下载 PNG
- 能下载 PDF
- 下载后的图片/PDF 与预览一致

## 九、推荐发布流程

第一次上线建议按这个节奏：

1. 部署到一个测试 HTTPS 地址
2. 在飞书自建应用中配置测试地址
3. 只开放给自己或一个小范围测试部门
4. 用 2-3 份真实 Excel 验收
5. 确认下载没有问题后，再扩大可用范围

## 十、后续可扩展能力

如果后续要做成更完整的内部工具，可以继续增加：

- 项目 JSON 保存/导入，方便下次继续编辑
- 本地化前端依赖，提升内网稳定性
- 飞书用户登录识别
- 自动保存导出文件到飞书云盘
- 导出后自动发送到指定飞书群

其中后三项需要服务端和飞书开放平台权限；当前纯静态版本先不接入这些能力。
