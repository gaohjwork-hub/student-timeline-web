# 飞书在线表格读取 + Netlify 发布完整指南

本文档用于发布新版“学生时间规划表图片生成器”：

- 支持本地上传 Excel
- 支持粘贴飞书在线电子表格链接
- 支持读取在线表格子表
- 支持选择子表生成时间规划图
- 支持继续编辑、拖拽、拉伸色块
- 支持导出 PNG/PDF

## 一、这版和纯静态版的区别

旧版是纯静态网页，只能上传本地 Excel。

新版增加了 Netlify Function：

```text
netlify/functions/feishu-sheets.js
```

它负责在服务器端调用飞书接口：

- 获取 `tenant_access_token`
- 获取电子表格子表列表
- 读取指定子表的 `A1:K120` 数据

这样做的原因：

- 飞书 `App Secret` 不能放在浏览器前端
- 在线表格读取需要飞书开放平台接口
- Netlify Function 可以安全读取环境变量

## 二、飞书自建应用配置

### 1. 创建企业自建应用

打开：

```text
https://open.feishu.cn/app
```

点击：

```text
创建企业自建应用
```

建议填写：

```text
应用名称：学生时间规划表生成器
应用描述：读取飞书在线表格或本地 Excel，生成可编辑时间规划图，并导出 PNG/PDF。
```

创建后记录：

```text
App ID
App Secret
```

后面要填到 Netlify 环境变量里。

### 2. 添加网页应用能力

在应用后台进入：

```text
应用能力 -> 添加应用能力 -> 网页应用
```

主页地址先留空，等 Netlify 发布完成后再回来填写。

### 3. 开通权限

在应用后台进入：

```text
权限管理
```

搜索并开通和以下含义对应的权限：

```text
读取电子表格
查看电子表格
查看云空间中文件
```

不同企业后台的权限名称可能略有差异。原则是：应用需要能够读取云文档里的电子表格内容。

如果只读不到子表或数据，通常是以下两类问题：

- 权限管理里没有开通电子表格读取权限
- 目标在线表格没有授权给这个应用或应用所在企业

### 4. 发布应用版本

权限调整后需要发布应用版本：

```text
应用发布 -> 版本管理与发布 -> 创建版本
```

版本说明建议写：

```text
支持读取飞书在线电子表格子表，并生成可编辑时间规划图。
```

提交后等待企业管理员审核。

## 三、飞书在线表格准备

在线表格结构建议和原 Excel 一致：

```text
第 1 行：标题
第 2 行：表头
A 列：阶段
B 列：年级/学期，例如 10年级暑期、10年级上学期、10年级寒假、10年级下学期
C 列：月份
D-K 列：任务栏目
```

新版页面会读取：

```text
A1:K120
```

如果你的表格超过 120 行，需要修改前端请求里的范围。

注意：

- 阶段列、年级列如果用合并单元格，飞书接口通常只返回合并区域第一行的值；页面会自动把下面的空白行延续成同一分段。
- 任务区域的跨月合并信息如果接口没有返回，页面会先生成单行色块。生成后可以在网页里拖拽拉伸色块。

## 四、Netlify 发布

### 1. 上传新版包

把新版 zip 上传到 Netlify。

操作路径：

```text
Netlify -> Add new site -> Deploy manually
```

拖入新版 zip 包。

### 2. 配置环境变量

进入 Netlify 站点后台：

```text
Site configuration -> Environment variables
```

新增：

```text
FEISHU_APP_ID=你的飞书 App ID
FEISHU_APP_SECRET=你的飞书 App Secret
```

保存后重新部署一次：

```text
Deploys -> Trigger deploy -> Deploy site
```

如果不重新部署，Netlify Function 可能读取不到新环境变量。

### 3. 回填飞书网页应用主页

Netlify 发布完成后，复制站点地址，例如：

```text
https://your-site-name.netlify.app/
```

回到飞书应用后台：

```text
网页应用 -> 主页地址
```

填写 Netlify 地址。

PC 端优先使用，移动端也可以填同一个地址。

## 五、使用方式

### 方式一：读取飞书在线表格

1. 打开应用页面
2. 粘贴飞书电子表格链接
3. 点击“读取子表”
4. 选择目标子表
5. 点击“生成表格”
6. 编辑、拖拽、拉伸色块
7. 下载 PNG 或 PDF

### 方式二：上传本地 Excel

旧流程保留：

1. 点击“上传 Excel”
2. 选择本地 `.xlsx`
3. 生成后编辑和导出

## 六、上线验收清单

发布后请用真实在线表格检查：

- 飞书应用能在工作台打开
- Netlify 页面能正常打开
- 粘贴飞书在线表格链接后能读取子表
- 能选择子表并生成计划图
- 标题、表头、阶段、年级、月份显示正常
- 底表按暑期、上学期、寒假、下学期分色
- 任务色块能编辑、拖动、拉伸、新增、删除
- PNG 能下载
- PDF 能下载

## 七、常见问题

### 1. 提示环境变量未配置

检查 Netlify 环境变量：

```text
FEISHU_APP_ID
FEISHU_APP_SECRET
```

保存后需要重新部署。

### 2. 能打开页面，但读取子表失败

检查：

- 飞书 App ID 和 App Secret 是否填错
- 飞书应用权限是否开通电子表格读取
- 应用权限调整后是否重新发布版本
- 目标在线表格是否允许应用读取

### 3. 读取到了子表，但生成内容为空

检查：

- 子表是不是目标子表
- 数据是否在 `A1:K120`
- 表头是否在第 2 行
- 月份是否在 C 列
- 任务栏目是否在 D-K 列

### 4. 跨月任务没有自动变成长色块

飞书读取单元格值接口通常不会直接返回任务区合并单元格信息。先生成单行色块，再在页面中拖拽拉伸即可。

## 八、后续可升级

后续如果要进一步自动化，可以继续做：

- 读取飞书表格合并单元格信息
- 自动保存编辑后的项目 JSON
- 将导出 PNG/PDF 自动上传到飞书云盘
- 导出后自动发送到指定飞书群

这些都需要继续扩展飞书 API 权限和 Netlify Function。
